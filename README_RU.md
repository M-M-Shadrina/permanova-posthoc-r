```markdown
# PERMANOVA + Post-hoc: интерактивный конвейер на R

**Автор:** Мария Шадрина  
**Контакт:** shadrina@mail.tsu.ru  
**Организация:** Томский государственный университет  

> 🇬🇧 [Read in English](README.md)

---

## Описание

Интерактивный R-скрипт для проведения **PERMANOVA** (`adonis2`, пакет vegan)  
с последующим **попарным пермутационным t-тестом** (post-hoc анализ)  
одновременно по нескольким зависимым переменным.  
Результаты экспортируются в файл **Excel** с четырьмя листами.

---

## Состав репозитория

| Файл | Описание |
|------|----------|
| `permanova_pipeline_en.R` | Основной скрипт (подсказки на английском) |
| `permanova_pipeline_ru.R` | Основной скрипт (подсказки на русском) |
| `table_example.csv` | Пример входного файла |
| `PERMANOVA_results.xlsx` | Пример результатов работы скрипта |
| `README.md` | Документация на английском |
| `README_ru.md` | Этот файл |

---

## Требования

| Пакет | Назначение |
|-------|-----------|
| `tidyverse` | Обработка данных |
| `vegan` | PERMANOVA (`adonis2`) |
| `RVAideMemoire` | Попарный пермутационный t-тест |
| `effectsize` | Размер эффекта (d Коэна) |
| `openxlsx` | Экспорт в Excel |

Установка всех пакетов сразу:

```r
install.packages(c("tidyverse", "vegan", "RVAideMemoire",
                   "effectsize", "openxlsx"))
Формат входных данных
CSV-файл, содержащий:

Столбцы-факторы — категориальные переменные группировки
(например, Group, Gender)
Столбцы-переменные — числовые зависимые переменные
(например, объёмы структур мозга)
Поддерживаемые разделители: , (запятая) или ; (точка с запятой).

Пример входной таблицы
Пример входной таблицы

Таблица содержит столбцы-факторы (Group, Age_Group, Gender)

и множество числовых переменных (объёмы структур мозга).

Запуск
Откройте R или RStudio.
Запустите скрипт:
r

source("permanova_pipeline_ru.R")
Следуйте интерактивным подсказкам:

Шаг 1 — путь к CSV-файлу
Шаг 2 — разделитель (1 = запятая, 2 = точка с запятой)
Шаг 3 — количество факторов (2 или 3) и их названия
Шаг 4 — количество зависимых переменных и их названия
Шаг 5 — подтверждение списка переменных
Шаг 6 — путь для сохранения (Enter = текущая папка)
Шаг 7 — метод поправки на множественные сравнения
Статистическая логика

Для каждой зависимой переменной:
│
├── PERMANOVA (adonis2, евклидово расстояние, 9 999 пермутаций)
│     Модель: переменная ~ фактор1 * фактор2 [* фактор3]
│     Результат: Df, SumOfSqs, R², F, p-значение, звёздочки
│     Поправка FDR по всем переменным (метод BH)
│
└── Post-hoc: попарный пермутационный t-тест (RVAideMemoire)
      Проводится внутри каждого уровня фактора 2
      Размер эффекта: d Коэна (объединённое SD)
      Поправка на множественные сравнения: на выбор пользователя
      Поправка FDR по всем сравнениям (метод BH)
Результаты
Скрипт сохраняет файл PERMANOVA_results.xlsx с четырьмя листами:

Лист	Содержимое
Primary_all	Полная таблица PERMANOVA по всем переменным
Primary_significant	Строки с FDR < 0.05
PostHoc_all	Все попарные сравнения
PostHoc_significant	Попарные сравнения с FDR < 0.05
Обозначения: * p < 0.05 · ** p < 0.01 · *** p < 0.001

Пример результата — основной анализ (PERMANOVA)
Результаты PERMANOVA

Лист Primary_all: результаты PERMANOVA для каждой структуры и терма —

R², F-статистика, p-значение и значимость с поправкой FDR.

Пример результата — post-hoc анализ
Результаты post-hoc

Лист PostHoc_all: попарные сравнения внутри каждого уровня фактора 2

с размером эффекта d Коэна, p-значением и поправкой FDR.

Примечания
Переменные с менее чем 12 наблюдениями автоматически пропускаются.
Если для какой-либо переменной возникает ошибка в adonis2
или post-hoc тесте, переменная пропускается, анализ продолжается.
Пользователи Windows могут вставлять путь с кавычками —
они удаляются автоматически.
Цитирование
Если вы используете этот скрипт в своей работе, пожалуйста,

сделайте ссылку на него:

Шадрина М. (2025). PERMANOVA + Post-hoc Pipeline [R-скрипт].
